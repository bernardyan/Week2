// export const guessRandom = num => {
//     const random = Math.floor((Math.random() * 10) + 1);
//     const diff = Math.abs(random - num);
//     const success = (random == num);
//     const message = `You guessed ${num}, whereas the target is ${random} with a difference of ${diff}`;
//
//     return {"input": num, "target": random, "diff": diff, "msg": message, "success": success};
// };